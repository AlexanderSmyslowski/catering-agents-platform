name: PR698 DDL guard RED-GREEN

on:
  push:
    branches:
      - codex/catering-target-update-path-20260921

permissions:
  contents: read

jobs:
  focused:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx vitest run tests/catering-target-production-controlflow.test.ts --maxWorkers=1
