# E2E Harness Chain

- Phase 1.1: prove the production draft chain through real HTTP routes.
- Scope: fixture draft -> import -> review decisions -> apply -> plan/purchase list/folder export.
- Acceptance: import remains draft-only until apply; every non-approved status rejects apply without state change; test runs in normal battery.
- No new API, no product semantics change.
