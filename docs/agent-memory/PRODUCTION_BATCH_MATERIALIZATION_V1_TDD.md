# Production Batch Materialization v1 TDD

## RED target
The first RED commit adds only the materialization contract tests. No production implementation exists yet.

Expected RED cause: `materializeProductionBatchFromBridge` is missing from the public shared-core surface.
