# Production Intake & Clarification v1 — TDD

Stand: RED.

Branch: `feature/production-intake-clarification-v1`.

Der Slice definiert zunächst nur den erwarteten Readiness-Vertrag. Produktcode für `evaluateProductionIntakeReadiness` ist absichtlich noch nicht vorhanden.

RED-Erwartung: Build/Test scheitert, weil der neue Export noch fehlt.

Kein LLM-/Provideraufruf, keine Migration, kein Deployment, keine echten Kundendaten.
