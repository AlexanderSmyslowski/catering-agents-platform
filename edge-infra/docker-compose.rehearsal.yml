services:
  edge:
    environment:
      CATERING_UPSTREAM: https://web:443
    ports: !override
      - "127.0.0.1:18080:80"
    volumes: !override
      - ./Caddyfile.rehearsal:/etc/caddy/Caddyfile:ro
      - edge_caddy_data:/data
      - edge_caddy_config:/config
