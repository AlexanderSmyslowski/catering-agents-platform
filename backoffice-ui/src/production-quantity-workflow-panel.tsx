import { useEffect, useState } from "react";
import {
  confirmProductionQuantityOverride,
  fetchProductionQuantityWorkflow,
  previewProductionQuantityOverride,
  type ProductionQuantityWorkflowEdit,
  type ProductionQuantityWorkflowItem,
  type ProductionQuantityWorkflowPreviewResponse
} from "./production-quantity-api.js";

type Props = { caseId?: string };
type PendingPreview = { component: ProductionQuantityWorkflowItem; edit: ProductionQuantityWorkflowEdit; response: ProductionQuantityWorkflowPreviewResponse };
function numberLabel(value: number | undefined, unit: string | undefined): string { return value === undefined ? "–" : `${value} ${unit ?? ""}`.trim(); }
export function ProductionQuantityWorkflowPanel({ caseId }: Props) {
  const [items, setItems] = useState<ProductionQuantityWorkflowItem[]>([]); const [loading, setLoading] = useState(false); const [error, setError] = useState<string>(); const [notice, setNotice] = useState<string>(); const [pending, setPending] = useState<PendingPreview>(); const [draftValues, setDraftValues] = useState<Record<string, string>>({});
  const reload = async () => { if (!caseId) { setItems([]); return; } setLoading(true); setError(undefined); try { const result = await fetchProductionQuantityWorkflow(caseId); setItems(result.items); } catch (cause) { setError(cause instanceof Error ? cause.message : "Mengen konnten nicht geladen werden."); } finally { setLoading(false); } };
  useEffect(() => { void reload(); }, [caseId]);
  const previewTarget = async (item: ProductionQuantityWorkflowItem) => { if (!caseId || !item.currentAuthority) return; const amount = Number(draftValues[`target:${item.componentId}`]); if (!Number.isFinite(amount) || amount <= 0) { setError("Bitte eine positive Menge eingeben."); return; } setError(undefined); setNotice(undefined); try { const edit: ProductionQuantityWorkflowEdit = { origin: "target_output", perUnitAmount: amount, unit: item.currentAuthority.unit }; const response = await previewProductionQuantityOverride(caseId, item.componentId, edit); setPending({ component: item, edit, response }); } catch (cause) { setError(cause instanceof Error ? cause.message : "Mengenvorschau konnte nicht erstellt werden."); } };
  const previewPurchase = async (item: ProductionQuantityWorkflowItem, rowId: string) => { if (!caseId) return; const row = item.purchaseRows.find((candidate) => candidate.rowId === rowId); if (!row?.editable || !row.lineage) return; const amount = Number(draftValues[`purchase:${rowId}`]); if (!Number.isFinite(amount) || amount <= 0) { setError("Bitte eine positive Einkaufsmenge eingeben."); return; } setError(undefined); setNotice(undefined); try { const edit: ProductionQuantityWorkflowEdit = { origin: "purchase_ingredient", ingredientId: row.lineage.ingredientId, amount, unit: row.unit }; const response = await previewProductionQuantityOverride(caseId, item.componentId, edit); setPending({ component: item, edit, response }); } catch (cause) { setError(cause instanceof Error ? cause.message : "Einkaufsmengen-Vorschau konnte nicht erstellt werden."); } };
  const confirm = async () => { if (!caseId || !pending || pending.response.preview.status !== "preview_ready") return; setError(undefined); setNotice(undefined); try { const result = await confirmProductionQuantityOverride(caseId, pending.component.componentId, pending.response.previewId, pending.edit); setPending(undefined); setDraftValues({}); setNotice(result.status === "review_required" ? "Mengenänderung gespeichert. Die neue Menge benötigt noch die Küchenfreigabe." : "Mengenänderung übernommen und abhängige Produktionsmengen neu berechnet."); await reload(); } catch (cause) { setError(cause instanceof Error ? cause.message : "Mengenänderung konnte nicht übernommen werden."); } };
  if (!caseId) return null;
  return <article className="panel secondary-panel" data-testid="production-quantity-workflow"><header><p className="eyebrow">Mengensteuerung</p><h3>Produktionsmengen prüfen und ändern</h3></header><p className="helper-text">Änderungen werden zuerst vollständig vorgerechnet. Rezept und Einkauf bleiben bis zur Bestätigung unverändert.</p>{loading ? <p className="helper-text">Mengen werden geladen …</p> : null}{notice ? <p className="helper-text" role="status">{notice}</p> : null}{error ? <p className="helper-text" role="alert">{error}</p> : null}<ul className="item-list compact">{items.map((item) => <li key={item.componentId}><strong>{item.label}</strong><p>Aktuell: {numberLabel(item.currentAuthority?.perUnitAmount, item.currentAuthority?.unit)} pro Person · Gesamt {numberLabel(item.currentAuthority?.targetAmount, item.currentAuthority?.unit)}</p>{item.currentAuthority?.reviewStatus === "kitchen_review_required" ? <p className="helper-text">Bestätigte Mengenänderung · Küchenfreigabe noch erforderlich.</p> : null}{item.status === "recommended" ? <p className="helper-text">Empfehlung: {numberLabel(item.recommendedAmount, item.unit)}{item.professionalRange ? ` · professioneller Korridor ${item.professionalRange.min}–${item.professionalRange.max} ${item.professionalRange.unit}` : ""}</p> : <p className="helper-text">{item.reviewMessage ?? "Küchenentscheidung erforderlich."}</p>}{item.canEdit && item.currentAuthority ? <div><label>Menge pro Person ändern<input aria-label={`${item.label} Menge pro Person`} inputMode="decimal" value={draftValues[`target:${item.componentId}`] ?? String(item.currentAuthority.perUnitAmount ?? "")} onChange={(event) => setDraftValues((current) => ({ ...current, [`target:${item.componentId}`]: event.target.value }))}/></label><button type="button" onClick={() => void previewTarget(item)}>Änderung prüfen</button></div> : null}{item.purchaseRows.length > 0 ? <details><summary>Einkaufspositionen</summary><ul className="item-list compact">{item.purchaseRows.map((row) => <li key={row.rowId}><strong>{row.articleName}</strong> · {row.amount} {row.unit}{row.editable ? <div><input aria-label={`${row.articleName} Einkaufsmenge`} inputMode="decimal" value={draftValues[`purchase:${row.rowId}`] ?? String(row.amount)} onChange={(event) => setDraftValues((current) => ({ ...current, [`purchase:${row.rowId}`]: event.target.value }))}/><button type="button" onClick={() => void previewPurchase(item, row.rowId)}>Einkaufsmenge prüfen</button></div> : <p className="helper-text">{row.readOnlyReason}</p>}</li>)}</ul></details> : null}</li>)}</ul>{pending ? <section aria-label="Mengenvorschau"><h4>Auswirkung der Änderung</h4>{pending.response.preview.status === "preview_ready" ? <><p>Neu: {numberLabel(pending.response.preview.resultingTarget?.amount, pending.response.preview.resultingTarget?.unit)} · Faktor {pending.response.preview.scaleFactor}</p><ul className="item-list compact">{(pending.response.preview.recipeChanges ?? []).map((change) => <li key={change.ingredientId}>{change.name}: {change.baselineAmount} → {change.effectiveAmount} {change.unit}</li>)}</ul>{(pending.response.preview.appliedRuleIds ?? []).length > 0 ? <p className="helper-text">Freigegebene nichtlineare Regeln berücksichtigt: {pending.response.preview.appliedRuleIds?.join(", ")}</p> : null}<button type="button" disabled={!pending.response.preview.confirmable} onClick={() => void confirm()}>Änderung übernehmen</button></> : <p className="helper-text">Änderung kann nicht bestätigt werden: {(pending.response.preview.issues ?? []).join(", ")}</p>}<button type="button" onClick={() => setPending(undefined)}>Abbrechen</button></section> : null}</article>;
}
